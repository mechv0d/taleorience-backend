services:
  api:
    build:
      context: .
    image: ghcr.io/mechv0d/taleorience-backend:${TAG:-dev}
    ports:
      - "${PORT:-4000}:4000"
    environment:
      NODE_ENV: production
      PORT: 4000
      APP_MODE: local
      AUTH_MODE: none
      DATABASE_URL: sqlite://./data/taleorience.db
      STORAGE_DRIVER: local
      STORAGE_ROOT: ./storage
      LOCALES_ROOT: /app/locales
    volumes:
      - api_data:/app/data
      - api_storage:/app/storage
    restart: unless-stopped

volumes:
  api_data:
  api_storage: